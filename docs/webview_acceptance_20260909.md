# WebView 短时回归：交互阻断项（2026-09-09）

> 用户随后说明上一轮也在操作本机，因此此前样本存在键鼠/剪贴板干扰，不能单凭它们归因产品故障。
> 17:21 请求暂时停止操作后重测，见文末；同样失败，但具体原因仍未确定。

本轮是验收/诊断，不是修复完成报告。约 16:57–17:06（UTC+8），最长单批 102 秒，
均含实例收尾、不超过 10 分钟。未修改产品代码、编译或发布运行文件。

## 范围与结论

本机 `build_official/dist/px_client.exe` 参数启动，Console 私有 CA 验证，
使用既有 `rtc-accept-webview-90` / node `rtc-accept-node-90` / 90:32010。
数据库保持 `entry_url=https://www.baidu.com/`、30 FPS、H.264，未改测试 URL 或创建额外账号。

| 项目 | 实机证据 | 判定 |
|---|---|---|
| 页面显示 | 已查看目标 Client HWND 的 1382×807 截图，百度页面可见 | 通过 |
| 鼠标点击 / 普通文本输入 | 点击输入框后输入 `GammaRay keyboard 20260909`，截图可见完整字符串 | 此样本通过 |
| Ctrl+C 复制 | `WEB_COPY_ASCII=False`，未获得预期文本 | 失败，尚未定位具体层 |
| Ctrl+V 中文粘贴及再次复制 | 本机放入独立中文测试串，远端截图仍为原 ASCII；用新 sentinel 再复制，roundtrip=False | 失败 |
| Enter / 搜索 | 更换为显式虚拟键按下/释放后，仍停留在提示按回车的页面 | 未达到预期，需排查按键路径 |
| 滚轮 | 已发送，但当前页面没有形成可验证的滚动前后变化 | 未通过验收，不能直接认定滚轮损坏 |
| 断开后状态 | Client 关闭后观察 35 秒实例仍 running，随后显式停止并重复停止均 stopped | 符合既有 WebView 不以 RDP 宽限退出验收的边界；不据此证明媒体确已暂停 |
| 同实例重连 / 动态 WebGL | 遇到输入阻断后没有继续扩展 | 未测，不以多次新启动代替重连 |

本轮不能宣布 WebView 全面可用。尤其“能输入文字”不代表物理按键/组合键已经通过。

## 批次

| 本机私有记录目录 | 实例 | 秒数 | 说明 |
|---|---|---|---|
| app-cli-20260909085728 | inst-179-d9214426 | 102.02 | 页面、点击和普通输入通过；手动追加剪贴板步骤时客户端已按限时关闭，未执行 |
| app-cli-20260909090014 | inst-183-ff24f111 | 52.87 | 自动化助手 SendKeys 组合字符串报错，非产品失败依据 |
| app-cli-20260909090145 | inst-187-0f505a35 | 83.54 | 拆开组合键后复制/粘贴断言为 False；finally 还出现恢复剪贴板错误，属于失败样本 |
| app-cli-20260909090457 | inst-191-444a1fc9 | 73.62 | 显式 Ctrl/字母/Enter 按下释放仍复现；最终明确因剪贴板断言失败退出 |

图片仅由精确 Client PID + HWND 的 PrintWindow 捕获，没有桌面区域复制回退。
执行输入前通过 UI Automation 激活并确认前台；最后一批不再以 SendKeys 的组合字符串作为唯一模拟方式。
剪贴板测试仅使用测试字符串；助手后来改为预先物化 DataObject 格式数据以避免依赖失效的原 OLE 对象。
首次剪贴板恢复报错的样本不能声称已确认原剪贴板所有格式恢复成功。

关闭均出现明确退出确认，未强杀 Client，退出码为 1；不能将它写成退出码 0。
动态脚本装配下 `NORMAL_CLIENT_EXIT` 汇总变量出现作用域问题，不能独立作为退出异常或正常的证据；
本轮保留逐项退出确认、进程退出和服务端最终状态，而不是用该汇总布尔量掩盖输入失败。

## 定位入口（未证明根因）

- Client `front_render/ct_video_widget.cpp` 分开发送 `kKeyEvent` 与 `kTextInput`；
  普通文本成功、组合键和 Enter 未成功，需要检查实际发出的 VK、down/up 顺序及接收端处理。
- Render `webview/webview_runtime.cpp::SendKeyOnUi` 使用 CEF RAWKEYDOWN/KEYUP，
  `SendTextOnUi` 单独投递 CHAR。应对比两条路径，不能直接把问题归咎于编码或网页。
- 共享剪贴板还涉及 `ingress/network_event_ingress.cpp` 的 user-proxy 路由；
  必须核对本次 App Render 的实际连接/权限/转发，而不是仅看源码分支就宣称断链。
- 本轮 stdout 抽取未提供足以归因的完整按键链路日志，没有完成源头到 CEF 的事件对照。

下一步优先做有界日志定位，明确键事件/组合键与剪贴板各自原因后再实施修复；
修复后复验输入、滚动、同实例重连和动态页面。不要跳过此阻断项继续累计“全部通过”。

## 收尾

四个精确实例均为 stopped、error 为空，显式停止和重复停止成功。数据库原 URL/FPS 保持不变。
90 仅剩 Session 1 桌面 Render PID 10748，无本轮 WebView App Render / FreeRDP 代理；
Administrator Session 1、B Session 2、A Session 3 均保留。
未注销、重启、删除账号/profile 或迁移备份，未清理其他工作区应用。

## 17:21 用户提示操作干扰后的复测

用户说明上一轮也在使用本机，要求重测。开始前明确请求测试期间暂不操作键鼠/剪贴板，
沿用原 URL、应用和同一套带前台检查的脚本，未修改产品或应用配置。

- `app-cli-20260909092116` / `inst-195-60bcedcf`，Client PID 17896，70.99 秒含收尾。
- 普通文字输入仍可见；`WEB_COPY_ASCII=False`、`WEB_CLIPBOARD_UNICODE_ROUNDTRIP=False`。
  `web-pasted.png` 已查看，输入框仍为原 ASCII 文本，不是预期中文。
- 未触发失去前台检查；这只证明脚本检查点上的焦点，不宣称已经独立监测或排除所有外部输入。
  本轮没有出现剪贴板恢复异常，最终因明确的剪贴板断言失败而退出。
- Client 退出确认完成，ExitCode=1 / Forced=False；此前动态脚本汇总布尔作用域限制仍保留。
  实例显式停止及重复停止成功，Console 最终 stopped / error 空。
- 客户端关闭后已通知用户可以恢复操作。没有扩展测试或改动 Windows 会话。

结论：重新安排短批次后仍可复现，不能简单认定上一轮失败全部由用户操作造成；
但也不能仅凭这套模拟输入证明产品代码是根因。下一步应使用可控页面与逐层按键日志，
或精确受控的真实按键对照，区分测试注入、Client、Render、CEF 及剪贴板同步问题。

## 20:51–20:54 光标修复后的输入复测

后续修复与通过结果见 [WebView 编辑快捷键与文本剪贴板修复](webview_clipboard_fix_20260909.md)：
21:54 批次 Ctrl+A 完整替换/回读、英文反向复制、中文双向文本剪贴板通过。
下述失败保留为修复前证据，不代表当前部署状态。

用户已手动确认光标正常，并授权自行测试。两轮保留原百度 URL、30 FPS 与 32010 节点配置，
未修改产品代码、数据库或 Windows 会话；测试前请用户暂不操作本机。

- `app-cli-20260909125137` / `inst-211-5e0521e2`：69.42 秒。
- `app-cli-20260909125311` / `inst-215-7323b0a3`：69.71 秒。
- 普通英文/数字输入可见；第一轮 Enter 后实际出现搜索结果，滚轮后内容及滚动条位置向下移动。
  因此旧记录中的 Enter/滚动未验证不能继续当作当前必现故障。
- 两轮复制 ASCII 与中文粘贴回读均失败。第二轮增加 `web-selected.png`，Ctrl+A 后未见全选高亮，
  不能将双向复制失败全部归为剪贴板同步；客户端日志证明 Ctrl/A/C/V 的 down/up 已投递。
- 90 的 `C:\Users\Public\Pixels\px_logs\pixels_render_32010.log` 在两轮对应时段均明确记录
  `user-proxy not connected, drop client clipboard`。这确认本机到远端剪贴板消息在 Render 被丢弃；
  不证明 CEF 快捷键处理正常，也不能单独解释反向复制失败。
- 测试后本机剪贴板恢复步骤未报错，两个 Client 都经退出确认关闭（ExitCode=1，Forced=False）。
  两个实例均经精确显式停止与重复停止返回 stopped；未声称 35 秒观察内自然退出通过。
- 已通知用户恢复操作。下一步分别排查 WebView Ctrl 快捷键与缺失的 user-proxy 剪贴板通路。
