# RDP 本地 profile 恢复后的交互验收（2026-09-09）

本机 dist Client → Console / WebSocket / proxy → 10.0.0.90，既有 A 应用 `app-24-8cf70180`、
端口 32014、原 SID 尾号 1091 / Session 3。参数启动客户端，授权信息仅 stdin，不点击连接。
测试窗口约 16:24–16:33（UTC+8），单批最长 102 秒，含收尾均小于 10 分钟。
未编译、发布或修改产品代码，只修正忽略目录内的测试脚本并更新文档。

## 结果

| 项目 | 本轮证据 | 结论/边界 |
|---|---|---|
| 键盘、组合键、身份 | Win+R、输入命令、Enter；截图 whoami 为 A 原账号 | 通过，未操作管理员桌面 |
| 中文 / emoji 文本剪贴板 | 远端 `CLIPBOARD_IN=True` 截图；本机精确匹配返回文本 True | 双向通过 |
| HTML / 图片剪贴板 | 双向 HTML 标识匹配；16×16 图片尺寸与指定像素颜色校验全部 True | 双向通过，不等于文件传输 |
| 系统音频 | 远端仅播放合成测试音；指定 Client PID 音频基线 0、峰值 0.24637，有效采样 35 次 | 输出链路通过，不是人工听感、音画同步或麦克风验收 |
| 远端分辨率 | 日志 `rdp.desktop.resize`：1280×720 → 1024×640 → 1920×1080 | 通过，不只是拉伸本地窗口 |
| 全屏、最小化恢复 | 截图 1920×1080、远端桌面可见，恢复后仍显示；正常关闭 Client | 通过，不等于多显示器 / 混合 DPI |
| 鼠标 / 滚轮 / 按键 | 专用远端窗口记录 clicks=1、wheels=1、keys=1 | 三项通过；未测试拖拽或相对鼠标游戏输入 |
| 动态画面、色块、中文 | 两张目标 HWND 截图帧号 191 / 315，圆形位置变化，RGB 条与中文清晰可辨 | 短时目视通过，不是像素级画质评估 |
| 动态呈现速率 | 1280×720 活跃阶段客户端约 5 秒窗口 presentation 统计 44.00–48.82 FPS | 未达到 60 FPS 证据；不是编码器极限、网络基准或显示器实际刷新率 |
| 解码与关闭 | 三个成功批次已知 AVC444 / DecodeFrame2 错误匹配均为 0；后两批明确 ExitCode=0、Forced=False | 当前短样本通过，不替代长期稳定性 |

批次索引（图片和完整日志仅在本机忽略的 `.cache/rdp_product_trust/` 保留）：

- 文本/键盘：`run-20260909082736` / `inst-161-1ab8c5f4`，40 秒。
- 富剪贴板/音频/显示：`run-20260909082923` / `inst-164-99c21144`，102 秒，Client 正常退出。
- 动态/鼠标：`run-20260909083141` / `inst-167-de785957`，49 秒，Client 正常退出。
  远端 fixture 自动在 30 秒关闭，报告 fixture_frames=1198；此计数不是客户端 FPS。

## 测试工具问题与失败样本

最初的 `inst-155-882d52bb`（48 秒）及 `inst-158-4b95b2c5`（7 秒）不算交互通过。
截图助手此前改为仅 PrintWindow 捕获目标 HWND，不再隐式激活窗口，而输入助手仍依赖该副作用。
第一批 whoami 图片仍是运行对话框，随后全屏前台检查中止；第二批明确在发送按键前中止。
本机 OpenInputDesktop 成功，前台为 WindowsTerminal，并非证据指向锁屏或 RDP 输入故障。

测试助手改为精确 Client PID + LargestWindow、显式激活及 UI Automation SetFocus，
验证前台后才发输入。截图仍仅使用 PrintWindow，无桌面区域复制回退。
随后的真实远端输出及输入计数才作为通过证据。没有为此修改或放宽产品输入逻辑。

## 收尾

- 各批 finally 停止自己的产品实例；只剩 Session 1 的桌面 Render PID 10748。
  Administrator Session 1、B Session 2、A Session 3 均保留；没有注销、重启或终止工作区既有应用。
- 精确清理本轮三个远端 fixture 目录（rich/audio/dynamic），删除前验证完整路径在 A 的 Temp 内、
  不是 reparse point、只含本轮 fixture.ps1 / result.json；未扫描或删除旧测试及用户文件。
  临时测试数据已删除，无远端恢复副本；结果保留在本记录及本机测试输出。
- 图形 fixture 自行关闭，测试命令窗口发送 exit 收尾；Client 无残留。
- 数据库启动参数未改。原 FSLogix 容器、迁移备份、A/B 本地 profile 未清理。

## 仍未完成

此次没有重测文件/目录剪贴板传输、独立文件管理器，也没有测 WebView 动态页面与交互。
麦克风、打印机、智能卡、多屏、触控/笔等仍按 P6 分项，当前无通过声明；
USB/手柄不在这轮范围，不因底层 RDP 协议支持就宣称产品已支持。
后续应单列文件传输和 WebView 短批次；60 FPS、音画同步及跨重启应用状态另设明确门禁。
