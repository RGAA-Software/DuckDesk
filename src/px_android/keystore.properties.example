# Copy to keystore.properties (ignored by Git) or use the PIXELS_* environment
# variables documented in Readme.md. Never commit a production keystore or its
# credentials.
storeFile=C:/secure/pixels-release.jks
storePassword=replace-me
keyAlias=pixels
keyPassword=replace-me
