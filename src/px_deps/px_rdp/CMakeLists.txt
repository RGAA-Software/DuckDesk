add_library(px_rdp_stream STATIC rdp_stream_packet.cpp rdp_tcp_bridge.cpp rdp_proxy_policy.cpp)
if(WIN32)
    target_sources(px_rdp_stream PRIVATE rdp_workspace_lease.cpp rdp_proxy_process.cpp rdp_private_directory.cpp rdp_client_endpoint.cpp)
    target_link_libraries(px_rdp_stream PUBLIC crypt32 iphlpapi advapi32)
endif()
target_compile_features(px_rdp_stream PUBLIC cxx_std_23)
target_include_directories(px_rdp_stream PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/..)
target_link_libraries(px_rdp_stream PUBLIC px_message px_common_async)
if(TARGET OpenSSL::SSL)
    target_link_libraries(px_rdp_stream PUBLIC OpenSSL::SSL OpenSSL::Crypto)
endif()
set_target_properties(px_rdp_stream PROPERTIES AUTOMOC OFF AUTOUIC OFF AUTORCC OFF)

if(BUILD_TESTING)
    find_package(GTest CONFIG REQUIRED)
    add_executable(test_rdp_stream tests/test_rdp_stream.cpp)
    target_link_libraries(test_rdp_stream PRIVATE px_rdp_stream GTest::gtest GTest::gtest_main)
    set_target_properties(test_rdp_stream PROPERTIES AUTOMOC OFF AUTOUIC OFF AUTORCC OFF)
    add_test(NAME rdp_stream COMMAND test_rdp_stream)
    set_tests_properties(rdp_stream PROPERTIES TIMEOUT 60 LABELS "rdp;render-lifecycle")
    add_executable(rdp_websocket_probe tests/rdp_websocket_probe.cpp)
    target_link_libraries(rdp_websocket_probe PRIVATE px_rdp_stream)
    set_target_properties(rdp_websocket_probe PROPERTIES AUTOMOC OFF AUTOUIC OFF AUTORCC OFF)
endif()
