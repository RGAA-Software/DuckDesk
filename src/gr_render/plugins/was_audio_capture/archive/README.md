# Archived WASAPI capture

Previous implementation of `IAudioCapture` using raw WASAPI loopback
(`AUDCLNT_STREAMFLAGS_LOOPBACK`).

Replaced by `miniaudio_audio_capture` (miniaudio WASAPI loopback). Kept for
reference / rollback; not built.
