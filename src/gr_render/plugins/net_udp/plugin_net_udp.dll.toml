#
author = "RGAA"

#
description = "UDP media channel (GameStream-style, raw UDP)"

#
version_name = "1.2.0"

#
version_code = 120

#
enabled = true

# udp listen port, 0 = use the unified udp-listen-port param from exe (default 20381)
listen-port = 0

# video FEC (Reed-Solomon) parity percent, 0 = disable (default 20)
fec-percent = 20
